import 'package:flutter/material.dart';

class DarkAppBar extends StatelessWidget {
  const DarkAppBar({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}